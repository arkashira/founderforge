# Idea Validator
A Python project for validating product ideas and generating market fit scores.

## Usage
Run the `idea_validator.py` script to calculate the market fit score for an example idea.

## Testing
Run the tests using `pytest` to ensure the implementation meets the acceptance criteria.
