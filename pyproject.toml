[project]
name = "founderforge"
version = "0.1.0"
description = "Interactive wizard for quick MVP setup (demo implementation)."
requires-python = ">=3.9"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
